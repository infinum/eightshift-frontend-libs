import { Meta } from '@storybook/blocks';
import * as ComponentData from './story.js';

<Meta of={ComponentData} name='About' />

# Card Block Variation - Simple Style

Block used to provide alternative block for card.

## Dependencies

1. custom/card

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_variation --name=card-simple
```
