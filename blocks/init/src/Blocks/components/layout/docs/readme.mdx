# Layout Component

A component that is used to provide a layout (grid or columns) functionality to your project.

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_component --name=layout
```
