import { Meta } from '@storybook/blocks';
import * as ComponentData from './story.js';

<Meta of={ComponentData} name='About' />

# Layout Component

Component used to provide layout grid/list functionality to your project.

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate blocks use-component --name=layout
```
