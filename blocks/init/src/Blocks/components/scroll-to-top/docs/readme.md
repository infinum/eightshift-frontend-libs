# Scroll To Top Component

Component used to provide scroll to top functionality to your project.

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_component --name=scroll-to-top
```
