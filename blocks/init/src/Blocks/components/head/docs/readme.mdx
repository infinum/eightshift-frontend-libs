import { Meta } from '@storybook/blocks';

<Meta title='Components/Head' />

# Head Component

Component used to provide head functionality to your project.

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate blocks use-component --name=head
```
