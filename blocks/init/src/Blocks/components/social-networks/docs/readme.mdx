# Social networks Component

Component used to provide links to social networks or an ability to share the current page on social networks.

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_component --name=social-networks
```
