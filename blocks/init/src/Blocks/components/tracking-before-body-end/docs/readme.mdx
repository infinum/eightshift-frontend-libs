# Tracking Before Body End Component

Component used to provide tracking before body end functionality to your project.

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_component --name=tracking-before-body-end
```
