# Accordion Block

Block used to provide accordion functionality to your project.

## Dependencies

1. components/accordion

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_block --name=accordion
wp boilerplate use_component --name=accordion
```
