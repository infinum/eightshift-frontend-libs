# Accordion Block

Block used to provide accordion functionality to your project. This is a block with inner blocks that hosts the functionality for Accordions.

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate blocks use-block --name=accordion
```
