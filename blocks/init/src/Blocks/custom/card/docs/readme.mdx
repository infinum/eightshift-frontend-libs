# Card Block

Block used to provide card functionality to your project.

## Dependencies

1. components/card

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate blocks use-block --name=card
wp boilerplate blocks use-component --name=card
```
