# Card Block

Block used to provide card functionality to your project.

## Dependencies

1. components/card

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_block --name=card
wp boilerplate use_component --name=card
```
