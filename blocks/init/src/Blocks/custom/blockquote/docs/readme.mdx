# Blockquote Block

Block used to provide quote functionality to your project.

## Dependencies

1. components/blockquote

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_block --name=blockquote
wp boilerplate use_component --name=blockquote
```
