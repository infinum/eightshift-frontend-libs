# Grid Preview Block

Block used to provide grid preview functionality to your project. This block will not show on frontend.
It is only used in block editor.

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_block --name=group
```
