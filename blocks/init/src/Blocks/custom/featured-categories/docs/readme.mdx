# Featured Categories Block

Block used to provide featured categories functionality to your project.

## Dependencies

1. components/card

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_block --name=featured-categories
wp boilerplate use_component --name=card
```
