# Link Block

Block used to provide link functionality to your project.

## Dependencies

1. components/link

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_block --name=link
wp boilerplate use_component --name=link
```
