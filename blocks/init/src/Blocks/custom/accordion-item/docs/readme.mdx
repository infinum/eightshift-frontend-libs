# Accordion item Block

Block used to provide accordion item functionality to your project.

## Dependencies

1. components/accordion-item

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate use_component --name=accordion-item
wp boilerplate use_block --name=accordion-item
```
