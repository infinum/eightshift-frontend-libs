import { Meta } from '@storybook/blocks';
import * as ComponentData from './story.js';

<Meta of={ComponentData} name='About' />

# Carousel item Block

Block used to provide carousel item functionality to your project.

## Dependencies

1. components/image

## Implementation

Open a terminal and type in this command inside your projects root:

```shell
wp boilerplate blocks use-component --name=image
wp boilerplate blocks use-block --name=carousel-item
```
