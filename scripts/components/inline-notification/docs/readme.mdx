import { Meta } from '@storybook/blocks';
import * as ComponentData from './story.js';

<Meta of={ComponentData} name='About' />

# Notification

A simple component to show editor notifications (most often for very simple messages and validation).

## Usage

```jsx
<Notification
	text={__('This is an error!', 'eightshift-frontend-libs')}
	type='error'
	showContrastOutline
/>
```
