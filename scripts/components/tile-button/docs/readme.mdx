import { Meta } from '@storybook/blocks';
import * as ComponentData from './story.js';

<Meta of={ComponentData} name='About' />

# TileButton

A Gutenberg `Button` with some preset classes to make it look specifc.

## Usage

```jsx
<TileButton
	label='abc'
	onClick={() => alert('Hi')}
/>
```
