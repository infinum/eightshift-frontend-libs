import { Meta } from '@storybook/blocks';
import * as ComponentData from './story.js';

<Meta of={ComponentData} name='About' />

# MatrixAlignControl

A universal component that returns either the `BlockAlignmentMatrixControl` (newer WP versions) or `BlockAlignmentMatrixToolbar` (older WP versions).

Use it just as you would the other controls, you'll get the one that works :)
